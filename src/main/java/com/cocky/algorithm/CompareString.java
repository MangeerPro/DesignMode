package com.cocky.algorithm;

import java.util.Arrays;

public class CompareString {
	public boolean compare(char[] str, char[] targetStr) {
		int length = str.length;
		int targetLength = targetStr.length;
		
		if(length != targetLength) {
			return false;
		}
		
		
		return true;
	}
}
